mixin highlight(content, icon, heading)
    article(class=`molecule-highlight`)
        h1(class="h1 molecule-highlight__heading") #{heading}
        span(class=`icon ${icon} molecule-highlight__icon`)
        p(class="p molecule-highlight__paragraph") #{content}